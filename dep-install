#!/bin/sh
# Ce script installe les dépendances de l'AUR.

sudo pacman -S - < ~/dotfiles/dep-pac.txt
git clone https://aur.archlinux.org/yay ~/yay
cd ~/yay
makepkg -si
yay -S - < ~/dotfiles/dep-aur.txt
