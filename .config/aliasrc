#!/bin/sh
alias ls="exa -l --git"
alias la="exa -l --git -a"
alias l="exa -l --git"
alias ms="exa -l --git"
alias ytv="youtube-viewer"
alias za="zathura"
alias p="pacman"
alias y="yay"
alias v="nvim"
alias vim="nvim"
alias zsource="source ~/.config/zsh/.zshrc"
alias sxiv="sxiv -g 640x480 "
alias mkdir="mkdir -p"
alias md="mkdir -p"
alias m="mkdir -p"
alias rm="rm -rf"
alias cp="cp -r"
alias ga="git add"
alias gm="git commit"
alias gpom="git push origin main"
alias s="sxiv"
alias cd="z"

for command in mount umount sv pacman updatedb su shutdown ; do
	alias $command="sudo $command"
done; unset command
