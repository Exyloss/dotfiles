add_newline=false

format = """
$cmd_duration
$directory$git_branch$git_commit$character"""

[character]
success_symbol = "[❯](bold green)"
vicmd_symbol = "[V](bold blue)"
error_symbol = "[❯](bold red)"

